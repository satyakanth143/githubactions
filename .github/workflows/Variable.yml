name: FIrst_Workflow

on:
  workflow_dispatch:

env:
  name: satya

jobs:
  say-hello:
    runs-on: ubuntu-latest
    env:
      lastname: vanga
    steps:
      - run: echo "Hi"
      - run: echo "${{ env.name }} and ${{ env.lastname }} and ${{ env.place }}"
        env:
          place: AMP
