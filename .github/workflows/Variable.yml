name: FIrst_Workflow

on:
  workflow_dispatch:
  env:
    name: satya

jobs:
  say-hello:
   runs-on: ubuntu-latest
   env:
     lastname: vanga
   steps:
   env:
     place: AMP
     - run: echo "Hi"
     - run : echo " $name and $lastname and $place"
